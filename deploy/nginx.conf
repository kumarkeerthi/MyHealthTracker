# Generated file placeholder.
# setup.sh will overwrite this file with the correct DOMAIN-specific config.

server {
    listen 80;
    server_name localhost;

    location / {
        return 200 'nginx config placeholder - run ./setup.sh --production';
        add_header Content-Type text/plain;
    }
}
