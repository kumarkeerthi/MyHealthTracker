# First-Time Docker Setup (Moved)

For a single, canonical setup flow, use:

- `SETUP.md`
- `./setup.sh`

Quick start:

```bash
./setup.sh
```

This brings up backend, frontend, and database together.

If you need production provisioning/deploy:

```bash
./setup.sh --production
```
